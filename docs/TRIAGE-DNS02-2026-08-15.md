# DNS-02 triage — glue oracle + malformed (2026-08-15)

Measurement notes only. No CVE / exploitable claims.

## Lab

| Field | Value |
| --- | --- |
| Host | **Lab Test Server** (`lab`) · Tailscale `100.110.54.48` |
| Work root | `/opt/atlas/repos/dns-stack-diff-harness` |
| Scope | Application-layer MITM under plain Docker |

## Oracle gap closed (instrument)

| Change | Why |
| --- | --- |
| `dig` / UDP parse **ADDITIONAL** A RRs (`owner\|ip`) | Client replies often strip glue; still record when echoed |
| Follow-up **glue cache-accept probe** (`ns.evil.test.` → `198.51.100.66`) | Surfaces whether a hop cached out-of-bailiwick ADDITIONAL |
| Oracle axes `additional` + `glue_cache_accept` on `P-GLUE-BAILIWICK` | AA-only noise no longer the only glue signal |

Smoke still uses `SMOKE_AXES` only (rcode / answers / hang). AA/RA remain informational for smoke.

## P-GLUE-BAILIWICK — triage stance

| Signal | Reading |
| --- | --- |
| AA-only divergence (dnsmasq AA vs Unbound not) | **Informational / Class C noise** for path-consistency — same as smoke flag noise under pair stand-ins |
| `glue_cache_accept` diverge (one hop answers `198.51.100.66`, other does not) | **Security-relevant measurement candidate** — triage as Class A/B only after root-cause (cache vs recurse-out vs NXDOMAIN). Do not auto-label |
| Both `glue_cache_accept=false` + empty client ADDITIONAL | Instrument saw injection path; neither stand-in reused glue — **pass on glue axis** for this pair |

## P-MALFORMED-RCODE — triage stance

Pinned 2026-07-18 pattern (and expected under truncate MITM):

| Hop | Typical observation |
| --- | --- |
| Unbound | `SERVFAIL` (usable DNS reply) |
| dnsmasq | dig hard-error / timeout (`dig exit 9`) — no usable reply |

**Classification (author triage, not auto):**

- This is a **real path-consistency signal** under truncated upstream bytes.
- Prefer **Class B (robustness / fail-closed divergence)** framing until disclosure review: one hop returns a named RCODE; the other yields client-visible hang/hard-error.
- Do **not** claim Class A (security-relevant cache poison / bypass) from truncate alone.
- Flag axes (aa/ra) that co-fire when one side has `rcode=null` are **secondary** to `rcode` + `hang_or_crash`.

## Restore rule

After `make adversarial`, topology restores to smoke (direct-to-auth). Re-run `make smoke` to confirm Class C harness still green.

## Still deferred (not this lab pass)

- Unbound **full recursive** mode (smoke stays forward-only)
- Auto Class A/B table / disclosure language
- OS-layer Klein / SAD DNS
